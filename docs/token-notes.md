# Учёт токенов Яндекс.Диска

> В этом файле НИКОГДА нет самих токенов — только процедура, даты и метаданные.
> Токены: серверный — в `/opt/echo-bot/yd-adapter/.env` (chmod 600) на <сервер>,
> телефонный — в защищённом хранилище клиента (flutter_secure_storage).

## OAuth-приложение

| Параметр | Значение |
|---|---|
| Название | echo-pult |
| Платформа | Веб-сервисы |
| Права | `cloud_api:disk.read`, `cloud_api:disk.write` |
| Redirect URI | `https://oauth.yandex.ru/verification_code` |
| client_id | `1abcf47c2f5e48ad9e116c86db5ff2cc` |
| Дата создания | 2026-09-18 |

## Процедура выдачи токена (implicit flow, вручную)

Факты из доки Яндекс ID (`yandex.ru/dev/id/doc/ru`, разделы «Получение токена вручную»
и «Время жизни токена»):

- страница согласия показывается **один раз** на пару «приложение + аккаунт»; дальше та же
  ссылка отдаёт токен молча. Согласие сбрасывается только отзывом доступа на
  `id.yandex.ru/personal/data-access`; смена пароля / «выйти на всех устройствах» отзывают
  токены, но не согласие — перевыдача снова без экрана;
- токен и срок жизни приходят в redirect-URL после `#`:
  `#access_token=…&expires_in=<секунд>`;
- типы токенов: «продлеваемый» (живёт «не менее 1 года», продлевается при каждом
  использовании) и «ограниченный» (фиксированный срок по правам). Что выпало для disk-прав —
  записать ниже;
- **без `device_id` токен один** на пару «приложение + аккаунт + права»: повторные выдачи
  возвращают ту же строку (проверено 18.09.2026). Чтобы токены сервера и телефона были
  независимы (решение 11 плана), выдаём их привязанными к «устройствам»:
  сервер — `&device_id=server-echo-144&device_name=server-144`,
  телефон — `&device_id=phone-echo&device_name=phone`;
- device-токены: лимит 30 на пользователя на приложение (новая выдача убивает самый старый);
  отзыв — вручную на id.yandex.ru или приложением:
  `POST https://oauth.yandex.ru/revoke_token` c `access_token` + `client_id`/`client_secret`.

Шаги:

1. Открыть `https://oauth.yandex.ru/authorize?response_type=token&client_id=<CLIENT_ID>&device_id=<ID>&device_name=<имя>`.
2. Разрешить доступ (один раз); из адресной строки или со страницы скопировать `access_token`
   и `expires_in`.
3. Проверить (не вставляя токен в чат/git):
   `curl -s -H "Authorization: OAuth $TOKEN" "https://cloud-api.yandex.net/v1/disk/" | head -c 400`
   — ожидаем JSON с `total_space`, `used_space`, `user.login`, не 401.
4. Если `expires_in` оказался неудобно коротким — перейти на explicit-flow (ниже).

> **Не вариант:** «пароль приложения» (id.yandex.ru) — он аутентифицирует только WebDAV
> (`webdav.yandex.net`). REST API (`cloud-api.yandex.net`) принимает исключительно OAuth
> и отвечает 401 на Basic-аутентификацию. Решение зафиксировано 18.09.2026.

## Explicit-flow (если implicit-токен оказался короткоживущим)

Серверный токен: `GET https://oauth.yandex.ru/authorize?response_type=code&client_id=…` →
обменять `code` на `https://oauth.yandex.ru/token` (POST: `grant_type=authorization_code`,
`code`, `client_id`, `client_secret`) → в `.env` сервера лежат `YD_TOKEN` + `YD_REFRESH_TOKEN`
+ `YD_CLIENT_ID` + `YD_CLIENT_SECRET` (600); адаптер сам обновляет access-токен по
`grant_type=refresh_token`. Телефонному токену explicit-flow не нужен — перевыдача вручную
приемлема.

## Выданные токены

| # | Для кого | device_id | Дата выдачи | expires_in / тип (продлеваемый/ограниченный) | Где живёт |
|---|----------|-----------|-------------|-----------------------------------------------|-----------|
| 1 | сервер (yd-adapter) | server-echo-144 | 2026-09-18 | 31536000 (1 год), explicit-flow, refresh_token получен (при первом обмене не сохранён; при необходимости перевыпустить — процедура выше) | `.env` на .144; локально пока `yd-adapter/.env` |
| 2 | телефон (echo-pult) | phone-echo | *(не выдан)* | — | secure storage клиента |

## A2. Доступность хостов с мобильного интернета телефона

Wi-Fi и VPN выключены. Ожидание: `cloud-api` — 401 (но грузится), `uploader`/`downloader` —
405/403/400, но НЕ таймаут.

| Хост | Отвечает (да/нет) | Что видел |
|---|---|---|
| cloud-api.yandex.net | | |
| uploader*.disk.yandex.net (конкретный из href) | | |
| downloader*.disk.yandex.net (конкретный из href) | | |

## A3/A4. Факты спайка (2026-09-18, ноут, токен server-echo-144)

| Проверка | Факт |
|---|---|
| smoke-цикл A3 | `ok: list=1 upload=201 duplicate=409 download=match delete=202` |
| Повторная загрузка по занятому имени | **409** (не 423) |
| Удаление папки | **202** — асинхронное, завершается в фоне |
| mkdir вложенного пути при отсутствии родителя | 409 — **Диск не создаёт родителей**, уровни создавать по одному |
| GET несуществующего пути | 404 `DiskNotFoundError` |
| Квота | ~60 из 252 ГиБ занято |
| Частота (A4) | 200 запросов подряд (~1 req/s), **429: 0** → при `POLL_INTERVAL=10` запас ×10; интервал 10 с оставляем |

## A4. Лимиты и квота

| Параметр | Значение |
|---|---|
| total_space / used_space | |
| 429 при цикле (сколько запросов до первого) | |
| Решение по интервалу опроса | 10 c / 30 c |

## Прочее

- Конфликт версий в облаке: правило протокола — никогда не перезаписывать (`overwrite=false`);
  фактический ответ на повтор по занятому имени — 409.
